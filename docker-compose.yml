services:
  paper-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: light-paper-web
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      RENDER_DISK_PATH: /data
    volumes:
      - ./render-data:/data
    restart: unless-stopped
